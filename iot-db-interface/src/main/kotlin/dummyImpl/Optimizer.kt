package dummyImpl

object Optimizer {
    fun extractTripleStoreAddresses(l: List<OperatorGraphPart>): List<Int> {
        return listOf()
    }
    fun optimize(s: String): OperatorGraph {
        return OperatorGraph()
    }
    fun split(o: OperatorGraph): List<OperatorGraphPart> {
        return listOf()
    }
    fun assignNodesToTripleStroceAccess(l: List<OperatorGraphPart>) {
// dies passiert zentralisiert, da die DB weiß, welche nodes es gibt, und wo was steht
    }
}